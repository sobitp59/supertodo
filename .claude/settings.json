{
  "permissions": {
    "allow": [
      "Bash(find /d/Coding/Products/desktop-apps/supertodoapp -maxdepth 3 -not -path */node_modules/* -not -path */target/* -not -path */dist/* -not -path */.git/* -type f -name *.ts -o -name *.tsx -o -name *.rs -o -name *.json)"
    ]
  }
}
